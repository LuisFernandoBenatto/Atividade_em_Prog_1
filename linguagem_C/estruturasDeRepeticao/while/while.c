#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){

    int num, contador;

    scanf("%d", &num);

    contador = 0;

    while (contador < num){
        printf("Nao devo pegar codigo da net e submeter no The Huxley\n");
        contador = contador + 1;
    }

    system("pause");
    return 0;
}
